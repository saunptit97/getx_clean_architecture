import 'package:flutter/material.dart';

class DefaultSeparatorWidget extends StatelessWidget {
  const DefaultSeparatorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}
